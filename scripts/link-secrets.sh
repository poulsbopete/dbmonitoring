#!/bin/bash
# DEPRECATED — DO NOT USE.
# updateCompleteTrack with partial fields wipes the full track content.
# Secrets are handled automatically by the $SECRET_NAME syntax in config.yml
# when instruqt track push runs. No post-push API call is needed.
echo "This script is deprecated and disabled. Use ./scripts/push.sh instead." >&2
exit 1
